/** Release switches. Changes require a new build and deployment. */
export const featureFlags = {
  // Keep disabled until the blog listing and articles have been reviewed.
  blogEnabled: false,
} as const;
